{"id":"plus-summary-001","category":"summary","input":"请总结下面内容，不超过三条并保留结论：学生工具 2.0 增加离线缓存和批量导入；旧版配置可自动转换；首次转换前建议备份。结论是已经满足发布条件，但仍需观察转换成功率。","expected_tier":"fast"}
{"id":"plus-summary-002","category":"summary","input":"请总结下面两份虚构记录并指出共同结论，不超过四条：记录 A：移动端首屏从 2.4 秒降至 1.7 秒，崩溃率保持 0.2%。记录 B：桌面端首屏从 1.8 秒降至 1.5 秒，崩溃率从 0.3% 降至 0.2%。","request":{"model":"auto","input":"请总结下面两份虚构记录并指出共同结论，不超过四条：记录 A：移动端首屏从 2.4 秒降至 1.7 秒，崩溃率保持 0.2%。记录 B：桌面端首屏从 1.8 秒降至 1.5 秒，崩溃率从 0.3% 降至 0.2%。","tools":[{"type":"function","name":"read_record"},{"type":"function","name":"compare_record"}]},"expected_tier":"balanced"}
{"id":"plus-summary-003","category":"summary","input":"请总结这段虚构的生产支付事故，不超过五条并保留处置结论：重复回调导致 17 笔订单被重复记账；系统在 8 分钟内关闭回调入口并回滚；所有受影响账目已复核；根因是幂等键未建立唯一约束；结论为修复约束后才能恢复入口。","expected_tier":"deep"}
{"id":"plus-rewrite-001","category":"rewrite","input":"请润色下面的产品提示，保持原意、语气友好并控制在 60 字以内：你的文件我们现在处理不了，你等一会再重新上传，不要一直点。","expected_tier":"fast"}
{"id":"plus-rewrite-002","category":"rewrite","input":"请把下面两段虚构通知合并改写为一段清晰公告，不超过 100 字：第一段：图书馆二层周六上午维护。第二段：维护期间自助借还机暂停，但一层人工柜台正常开放。","request":{"model":"auto","input":"请把下面两段虚构通知合并改写为一段清晰公告，不超过 100 字：第一段：图书馆二层周六上午维护。第二段：维护期间自助借还机暂停，但一层人工柜台正常开放。","tools":[{"type":"function","name":"read_notice"},{"type":"function","name":"merge_notice"}]},"expected_tier":"balanced"}
{"id":"plus-rewrite-003","category":"rewrite","input":"请改写下面的虚构生产安全公告，语气专业、事实不变：系统发现权限配置错误，已暂停外部访问。完成审计和修复验证前不得恢复服务。","expected_tier":"deep"}
{"id":"plus-code-explanation-001","category":"code_explanation","input":"请用通俗语言解释这段 Python 的执行流程，并指出返回值：\n```python\ndef unique_even(values):\n    seen = set()\n    return [x for x in values if x % 2 == 0 and not (x in seen or seen.add(x))]\n\nresult = unique_even([2, 3, 2, 4, 4, 6])\n```","expected_tier":"balanced"}
{"id":"plus-code-explanation-002","category":"code_explanation","input":"请解释下面 JavaScript 函数的返回值和可选链的作用：\n```javascript\nconst city = user => user?.profile?.address?.city ?? 'unknown';\nconst result = city({ profile: {} });\n```","expected_tier":"balanced"}
{"id":"plus-code-explanation-003","category":"code_explanation","input":"请从认证安全角度解释下面的伪代码为什么危险，并说明正确原则：\n```python\ndef is_admin(token):\n    payload = decode_without_verifying_signature(token)\n    return payload.get('role') == 'admin'\n```","expected_tier":"deep"}
{"id":"plus-debugging-001","category":"debugging","input":"这个 bug 请分析原因：下面 Python 代码为什么会在部分请求中出现 `RuntimeError: dictionary changed size during iteration`？请给出最小修复和一个回归用例：\n```python\ndef remove_expired(cache, now):\n    for key, item in cache.items():\n        if item.expires_at <= now:\n            del cache[key]\n```","expected_tier":"deep"}
{"id":"plus-debugging-002","category":"debugging","input":"这个 bug 请分析原因并给出最小修复：\n```javascript\nfor (var i = 0; i < 3; i++) {\n  setTimeout(() => console.log(i), 0);\n}\n```\n期望依次输出 0、1、2，实际输出 3、3、3。","expected_tier":"deep"}
{"id":"plus-debugging-003","category":"debugging","input":"这个 bug 请分析原因：下面 SQL 查询为什么遗漏没有订单的用户？请给出修复后的查询：\n```sql\nSELECT u.id, COUNT(o.id)\nFROM users u LEFT JOIN orders o ON u.id = o.user_id\nWHERE o.status = 'paid'\nGROUP BY u.id;\n```","expected_tier":"deep"}
{"id":"plus-architecture-001","category":"architecture","input":"为一个日活 5 万的校园预约系统设计可渐进落地的架构。要求支持登录、预约、防重复提交、取消和消息通知；高峰期每分钟约 2 万次预约请求；团队只有 4 名开发者。请说明核心组件、数据一致性策略、故障降级和第一阶段不该做什么。","expected_tier":"deep"}
{"id":"plus-architecture-002","category":"architecture","input":"设计一个供 20 人团队使用的内部文档审批模块，要求草稿、提交、两级审批、退回、操作审计和邮件提醒。请给出模块边界、核心数据模型、一致性策略和首版范围，避免过度设计。","expected_tier":"deep"}
{"id":"plus-architecture-003","category":"architecture","input":"评审一个把单体应用一次性拆成 15 个微服务的方案。团队有 5 人，当前日请求量 30 万，主要问题是发布互相影响。请给出风险、替代的渐进方案和可量化的拆分触发条件。","expected_tier":"deep"}
{"id":"plus-extraction-001","category":"extraction","input":"从下面文本提取 name、date、status、owner，严格返回一个 JSON 对象，不要补充不存在的信息：`项目“星桥”由李明负责，计划于 2026-08-12 发布；截至本周状态为等待负责人复核。`","expected_tier":"fast"}
{"id":"plus-extraction-002","category":"extraction","input":"从下面两条虚构记录中提取 id、owner、priority，按输入顺序返回 JSON 数组：`T-17 | owner=王宁 | priority=P1`；`T-18 | owner=赵可 | priority=P3`。","request":{"model":"auto","input":"从下面两条虚构记录中提取 id、owner、priority，按输入顺序返回 JSON 数组：`T-17 | owner=王宁 | priority=P1`；`T-18 | owner=赵可 | priority=P3`。","tools":[{"type":"function","name":"read_rows"},{"type":"function","name":"validate_rows"}]},"expected_tier":"balanced"}
{"id":"plus-extraction-003","category":"extraction","input":"从下面虚构的安全审计记录提取 system、severity、status，严格返回 JSON：`system=auth-gateway; severity=high; status=blocked_pending_review`。","expected_tier":"deep"}
